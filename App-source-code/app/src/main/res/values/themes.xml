<resources xmlns:tools="http://schemas.android.com/tools">

    <!--   ThemeDialog  -->
    <style name="dialogTheme" parent="@style/Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <!-- Used for the title and text -->
        <item name="android:textColor">?nsTitle</item>
        <item name="android:textColorAlertDialogListItem">?nsTitle</item>
        <item name="android:textColorSecondary">?nsTitle</item>
        <!-- Used for the background -->
        <item name="android:background">?nsDialogBackground</item>
        <!-- Used for the Button -->
        <item name="buttonBarNegativeButtonStyle">@style/ButtonStyle</item>
        <item name="buttonBarPositiveButtonStyle">@style/ButtonStyle</item>
    </style>

    <style name="ButtonStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">?nsTitle</item>
    </style>

    <style name="dialogAnimation">
        <item name="android:windowEnterAnimation">@anim/slide_up_dialog</item>
        <item name="android:windowExitAnimation">@anim/slide_down_dialog</item>
    </style>

    <style name="RatingBar_white" parent="Widget.AppCompat.RatingBar.Small">
        <item name="colorControlNormal">?nsTitleSub</item>
        <item name="colorControlActivated">?nsPrimary</item>
    </style>

    <style name="RatingBarDialog" parent="Theme.AppCompat">
        <item name="colorControlNormal">?nsTitleSub</item>
        <item name="colorControlActivated">#FF9800</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" >
        <item name="colorAccent">@color/white</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="TextViewNormal" parent="">
        <item name="android:textSize">14sp</item>
    </style>

    <style name="PopupMenuLight" parent="ThemeOverlay.AppCompat.Light"/>
    <style name="PopupMenuDark" parent="ThemeOverlay.AppCompat.Dark"/>

    <style name="CircularProgressWhite" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@color/white</item>
    </style>

    <style name="MyCheckBox" parent="Theme.AppCompat.NoActionBar">
        <item name="colorControlNormal">?nsTitle</item>
        <item name="colorControlActivated">@color/md_classic_accent</item>
        <item name="android:textColor">?nsTitle</item>
    </style>

    <style name="Theme.MiniPlayer" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="Widget.App.RadioButton" parent="Widget.Material3.CompoundButton.RadioButton">
        <item name="buttonTint">?nsTitle</item>
        <item name="buttonIconTint">?nsTitle</item>
    </style>

</resources>